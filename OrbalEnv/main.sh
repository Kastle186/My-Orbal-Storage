#!/usr/bin/env bash

# GENERAL TODO: Add safeguards where needed.

MAIN_SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
# results=$(find . -mindepth 2 -maxdepth 2 -type f -print)

echo 'Orbal Environment Main Script Under Construction!'
